```java title:"Tree to Single Linked List(Serialization)"
public class solution{
	public static TreeNode getTail(TreeNode node){  
	    TreeNode tail = node;
	      
	    while( node.children.size() > 0 ){  
	        node = node.children.get(0);  
	    }  
	      
	    return node;  
	}
	  
	public static void Serialize(TreeNode node){  
	    TreeNode secLastChild = node;  
	    for(var child : node.children){  
	        Serialize(child);  
	    }  
		
	    while(node.children.size() > 1){  
	        TreeNode last = node.children.remove(node.children.size() - 1);  
	        TreeNode secLast = node.children.get(node.children.size() - 1);  
	  
	        TreeNode secLastTail = getTail(secLast);  
	        secLastTail.children.add(last);  
	    }  
	}
}
```





