```java title:"Level of Binary Tree"
public static int Level(BinaryTree node){  
    int level = 0;  
  
    if(node == null)  
        return level;  
  
    level = Math.max(level,Level(node.left));  
    level = Math.max(level, Level(node.right));  
  
    return level+1;  
}
```
