### 🎯 Goal

Preorder traversal থেকে BST তৈরি করতে হবে।  
তুমি জানো preorder sequence হয়:

`Root → Left Subtree → Right Subtree`

এখন BST-এর একটা property হলো:

`সব Left Subtree < Root < সব Right Subtree`

তাহলে আমরা এমন একটা recursive structure বানাতে পারি, যেখানে প্রতিটা value তার range (min, max)-এর মধ্যে পড়ে কিনা, সেটা চেক করা হয়।

### 🧠 Intuition (why it works)

ধরা যাক preorder = `[8, 5, 1, 7, 10, 12]`

Traversal হবে এভাবে:

1. প্রথমে 8 root.
    
2. এরপর যেগুলো 8-এর চেয়ে ছোট — তারা left subtree.
    
3. যেগুলো 8-এর চেয়ে বড় — তারা right subtree.
    

আমরা recursion দিয়ে min–max range ধরে রাখি:

- শুরুতে `(min = -∞, max = +∞)`
    
- left child এর জন্য range হবে `(min, root)`
    
- right child এর জন্য `(root, max)`
    

এইভাবে preorder sequence-এর প্রতিটা element সঠিক BST position-এ যায়।

