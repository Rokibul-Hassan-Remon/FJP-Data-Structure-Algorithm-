## 🧩 লক্ষ্য (Goal)

Postorder traversal দেওয়া আছে  
👉 sequence: `Left → Right → Root`

তাহলে যদি আমরা postorder array-টা **পেছন দিক থেকে পড়ি**,  
তাহলে sequence effectively হয়:  
👉 `Root → Right → Left`

অর্থাৎ, এটা preorder traversal-এর “উল্টো” pattern।

তাহলে preorder-এর মতোই logic ব্যবহার করা যায়,  
কিন্তু **right subtree আগে**, তারপর **left subtree** বানাতে হবে।

## 🧠 Intuition

ধরা যাক postorder = `[1, 7, 5, 12, 10, 8]`

Traversal pattern:

`Left → Right → Root`

পেছন দিক থেকে পড়লে sequence হবে:

`Root → Right → Left`

তাই:

1. প্রথম element (8) root হবে।
    
2. এরপর 8-এর চেয়ে বড় value (10, 12) → right subtree
    
3. তারপর 8-এর চেয়ে ছোট value (1, 5, 7) → left subtree
    

এই pattern maintain করার জন্য আমরা recursion-এ `min`–`max` range track করি।

