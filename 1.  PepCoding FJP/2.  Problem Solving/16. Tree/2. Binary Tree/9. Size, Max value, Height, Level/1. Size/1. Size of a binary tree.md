```java title:"size of Binary tree"
public static int Size(BinaryTree node){  
    int count = 0;  
  
    if(node == null)  
        return count;  
  
    count += Size(node.left);  
    count += Size(node.right);  
  
    return count+1;  
}
```
