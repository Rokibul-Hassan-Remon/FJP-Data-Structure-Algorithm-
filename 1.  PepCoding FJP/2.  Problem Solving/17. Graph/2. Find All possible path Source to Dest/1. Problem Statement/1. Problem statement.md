# 🟦 Problem Description

### **Problem Title:**

Find All Paths from Source to Destination in a Graph

### **Problem Statement (Bangla + English Terms)**

তোমাকে একটি **undirected weighted graph** দেওয়া আছে।  
Graph-এ `N` vertices (0 থেকে N-1 পর্যন্ত numbered) এবং `M` edges আছে।  
প্রত্যেক edge-এর একটি weight দেওয়া আছে (weight শুধুমাত্র reference, path calculation-এর জন্য দরকার নেই)।

তোমার কাজ হলো:

1. **Check if a path exists** between a given **source** node `src` এবং **destination** node `dest`.
    
2. **Find and print all possible paths** from `src` to `dest`.
    
3. Graph এ **cycle থাকতে পারে**, তাই **same node এক path-এর মধ্যে duplicate হলে চলবে না**, কিন্তু অন্য path-এ আবার আসতে পারবে।
    

---

### **Input Format:**

1. প্রথম line: `N` → number of vertices
    
2. দ্বিতীয় line: `M` → number of edges
    
3. পরবর্তী `M` line: তিনটি integer `u v w`
    
    - u = source vertex
        
    - v = neighbour vertex
        
    - w = weight
        
4. পরবর্তী line: source এবং destination node (এক লাইনে বা দুইটি line-এ)
    

**Constraints:**

- 1 ≤ N ≤ 10⁴
    
- 0 ≤ M ≤ N*(N-1)/2
    
- 0 ≤ u, v < N
    
- 1 ≤ w ≤ 10⁵
    

---

### **Output Format:**

1. প্রথমে print করো:
    

`Source: src to Destination: dest Has Path? Yes/No`

2. তারপর print করো **all possible paths**, প্রত্যেকটি path একটি line-এ, vertices space-separated।
    

---

### **Example Input 1:**
```css
5
4
0 1 10
1 2 20
2 3 30
3 4 40
0
4
```


### **Example Output 1:**

```css
Source: 0 to Destination: 4 Has Path? Yes
All Possible Paths:
0 1 2 3 4
```

---

### **Example Input 2 (Graph with multiple paths):**

```css
4
5
0 1 1
0 2 1
1 2 1
1 3 1
2 3 1
0
3
```

### **Example Output 2:**

```css
Source: 0 to Destination: 3 Has Path? Yes
All Possible Paths:
0 1 2 3
0 1 3
0 2 1 3
0 2 3

```

---

### **Hints / Notes:**

- Use **DFS + Backtracking** to explore all paths.
    
- `isVisited` array ব্যবহার করতে হবে, এবং **backtracking এর সময় reset করতে হবে**।
    
- Weight input present থাকলেও, এই problem-এ **weight ignore** করা যাবে।
    
- Graph may contain **cycles**, তাই backtracking না করলে infinite recursion হতে পারে।