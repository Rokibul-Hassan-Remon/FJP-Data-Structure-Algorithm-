# 🟦 **Problem: Find a Path in an Undirected Graph**

### **Problem Statement**

তোমাকে একটি **undirected graph** দেওয়া আছে, যেখানে **N টি vertex** (labelled `0` to `N-1`) এবং **M টি edge** রয়েছে।

তোমার কাজ হচ্ছে—

👉 দেখানো যে **source node S থেকে destination node D পর্যন্ত**  
একটি **valid path** আছে কি না।

Graph-এ **cycle থাকতে পারে**, **self-loop থাকতে পারে**, কিন্তু এগুলো path খোঁজার ক্ষেত্রে অপশনাল।

---

### **Input Format**

- প্রথম লাইন দুটি integer:  
    `N` → number of vertices  
    `M` → number of edges
    
- পরবর্তী M টি লাইনে দুটি integer `u v` থাকবে  
    যা নির্দেশ করবে **u এবং v এর মধ্যে একটি undirected edge** আছে।
    
- শেষ লাইন দুটি integer:  
    `S` → source node  
    `D` → destination node
    

---

### **Output Format**

- যদি **S থেকে D পর্যন্ত** কোনো path থাকে ➝ print `"YES"`
    
- অন্যথায় ➝ print `"NO"`
    

---

### **Constraints**
```css
1 ≤ N ≤ 10^5
0 ≤ M ≤ 2 × 10^5
0 ≤ u, v < N
0 ≤ S, D < N

```

---

### **Example 1**

**Input**
```css
5
4
0 1 10
1 2 20
2 3 30
3 4 40
0
4
```

**Output**

```css
	Yes
```

---

**Explanation**  
Path exists: `0 → 1 → 2 → 3 → 4`

---

### **Example 2**

**Input**
```css
5 
2
0 1 10
2 3 20
0 
4
```

**Output**

```css
	No
```


**Explanation**  
0 থেকে 4 পর্যন্ত কোনো connectivity নেই।
