```java title:"Morris Traversal Thread Creation"

	public static void MorrisTraversalThread(BinaryTree node){  
	    BinaryTree current = node;  
	  
	    while(current != null)  
	        if(node.left != null){  
	            BinaryTree predecessor = node.left;  
	  
	            while(predecessor.right != null && predecessor.right != current){  
	                predecessor = predecessor.right;  
	            }  
	  
	            if(predecessor.right == null){  
	                predecessor.right = current;  
	                current = current.left;  
	            }  
	        }else{  
	            current = node.right;  
	        }  
		}
		
	}
```

